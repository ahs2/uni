<%= render(::Layouts::MainPageComponent.new(
  title: "Créé un utilisateur",
  class_name: "text-center"
)) do |component|  %>
  <% component.body do %>
    <div class="w-full lg:w-7/12 mx-auto">
      <turbo-frame id="user-form" target="_top">
        <%= render partial: 'users/form', locals: { user: @user } %>
      </turbo-frame>
    </div>
  <% end %>
<% end %>
